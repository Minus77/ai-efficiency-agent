# 中小企业 AI 提效场景识别 Agent — 架构设计（定稿 v1.2）

> 本文档整合五轮设计、一轮严苛自审与一轮细颗粒度拷问的全部结论，锁定 11 项架构决策 + 18 项细节决策。
> **v1.1 变更**：采集模型由「访谈驱动」改为「材料采集驱动」（见 §3、§12.2），
> 场景定义改为「业务结果父层 × 操作序列子层」两层结构（见 §11.1），新增客户准入标准（见 §17）。
> **v1.2 变更（闭合两处开放项）**：案例库来源重定位以消除 k-匿名风险（见 §15.2）；
> 新增受理前材料可得性探测与分级交付形态，将 C 级风险由交付事故转为明码交付形态（见 §17.1.1、§17.1.2）。
> 设计依据：Anthropic《Building Effective Agents》《Effective Context Engineering》《Writing Tools for Agents》、
> OpenTelemetry GenAI 语义约定（`gen_ai.*`，semconv 1.44）、Reflexion（arXiv 2303.11366）、
> ACE（arXiv 2510.04618）、Hermes Agent Self-Evolution（GEPA 反思式进化）、hermes-knowledge-curator（定期自学习编排）。

---

## 0. 决策记录

### 0.1 架构决策（第一轮，11 项）

| 编号 | 决策 | 选择 |
|---|---|---|
| Q0 | 立项前置 | **A** 先做两周基线实测，用结果决定是否投入建设 |
| Q1 | 使用形态 | **A** 顾问对外交付（多客户复用方法论 + 单客户强隔离） |
| Q2 | 数据接入 | **A+B 并行** L0 手工导入与 L1 只读 API 双轨，按客户条件择优 |
| Q3 | 交付物 | **A** 场景清单 + 优先级矩阵 + 分级 ROI + 90 天路线图 + 证据台账 |
| Q4 | 真值回访 | **工具层实现** 结果数据采集 + 用户反馈机制 + Agent 自主分析学习（Hermes 模式） |
| Q5 | Playbook 审核 | **人工 + 自动分级并行** 按风险分层，低风险自动、高风险人工 |
| Q6 | RAG 冷启动 | **A + 定期网络检索** 已有方法论文档导入，叠加周期性检索沉淀 |
| Q7 | 可观测性 | **A + 易用性优化** 接 OTel 后端，对外暴露自然语言可读信息 |
| Q8 | 严谨 vs 有用 | **A** 允许低证据高价值洞察，强制标注为专家判断并物理分区 |
| Q9 | 自述偏差纠偏 | **A+B+C 并行** 单据考古 + 多方交叉 + 可选工时记录，三路互校 |
| Q10 | 跨客户案例库 | **C→修正** 公开检索为主来源，客户侧只沉淀抽象方法论（v1.2 闭合，见 §15.2） |

### 0.2 细节决策（第二轮 grill-me，18 项）

**场景定义**

| 编号 | 议题 | 决策 |
|---|---|---|
| A1 | 场景切分规则 | **A+C 混合**：业务结果为父层、连续操作序列为子层（两层结构） |
| A2 | 场景数量 | 父层只讲最痛 2–3 条；客户要求时附加优先级靠后者，标注"备选" |
| A3 | 依赖场景 ROI | 只算独立可实现部分，依赖收益单列，汇总先去重再相加 |
| A4 | 多部门同类流程 | AI 工具化方案可复用则合并（强制标注收益构成），否则拆分 |
| A5 | "最痛"数哪一层 | 父层选最痛，**子层在父内穷尽**（依赖关系闭合在父内） |

**ROI 算法**

| 编号 | 议题 | 决策 |
|---|---|---|
| B1 | 人力成本单价 | 区间 + 公开行业基准，假设显式写明供客户校正 |
| B2 | 碎片时间折现 | 按**作业形态**判定：批量作业（同窗聚集）全额计入，真碎片不计入 |
| B3 | 实施成本 | 计入，给区间，算净收益与回本周期 |
| C1 | 作业形态判定证据 | 时间戳为主判据；**保留自述入口留作参考**；差异极大时人工介入 |
| I1 | 行业基准定位 | 只做横向对照，**不填 ROI 空缺** |

**采集模型**

| 编号 | 议题 | 决策 |
|---|---|---|
| G1 | 采集形态 | **改为材料采集驱动**，取消访谈（成本与协调开销过高） |
| H1 | 频次耗时来源 | **A+C 混合**：时间戳导出为唯一主力，轻量补数表兜底 |
| H2 | 材料收不齐 | 按证据空缺闭合率收口，**显式列出未获取材料及影响**；基准仅作参考 |
| D1 | 材料角色权重 | 不框死对象数量，做成自定义 + 角色权重，随迭代与行业调整 |
| D2 | 采集范围 | 不设轮次上限，改用**证据空缺闭合率**收敛 + 软提示 |
| D3 | 形式 | 文字为主 + 附件上传（多格式，图片走 OCR） |
| G5 | 附件安全 | 采纳：不可信数据、禁宏与公式、OCR 过注入检测、类型白名单 |

**能力判定与反馈**

| 编号 | 议题 | 决策 |
|---|---|---|
| E1 | 能力库组织 | 按能力类型建库（非产品），季度更新 + 版本化 |
| E2 | 是否点名产品 | 默认只给能力类型与选型标准；用户强烈要求时给最准答案并标"仅供参考" |
| F1 | 失败分类 | 三分类：漏判 / 高估 / 不可落地（归因与修正路径不同） |
| F2 | 即时反馈 | 留开放入口，任何角色可填，全部留痕；**角色必填** |
| F3 | 抗面子偏差 | 不问"准不准"，改问偏高/偏低与排序；叠加过程类业务指标 |
| G2 | 角色权重边界 | 可自演进，但**不得改变置信度分级规则**（分级口径仍冻结） |
| G3 | 反馈角色标签 | 角色必填，不同角色偏差方向已知可反向校正 |
| G4 | 业务指标选型 | 只用强直接关联的过程指标，**必须有改造前基线**；不用经营结果指标 |

**准入与商业**

| 编号 | 议题 | 决策 |
|---|---|---|
| I2 | 客户准入 | 硬门槛：至少 1 个在用系统且能导出；无系统者**直接拒接** |
| I3 | 规模行业 | 20–200 人；先聚焦 2–3 个熟悉行业 |
| J1 | 成本上限 | 不敏感，阈值定为正常成本 3–5 倍，用于**捕捉异常而非压成本** |
| J2 | 免责边界 | "决策参考，非投资承诺" + 首页假设清单 + 三条措辞纪律 |

---

## 1. 定级与前置门（Q0）

### 1.1 诚实的资格评估

Process-Fit 七维复评（修正首轮放水的打分）：

| 维度 | 判定 | 分 |
|---|---|---|
| 高频 | 单客户一次性，年频次几十次量级 | **0** |
| 多系统 | 横跨 IM/工单/ERP/表格/邮件 | 1 |
| 有 SOP | 提效评估方法论可编码 | 1 |
| 成功可测 | 延迟 3–6 个月且**归因不可能**（无反事实） | **0** |
| 混合任务 | 结构化取数 + 判断打分 | 1 |
| 错误可恢复 | 只读诊断 | 1 |
| 数据可得 | 中小企业数据最脏最散 | 0.5 |

**4.5/7 — 可做但需准备，且命中一项硬否决（成功标准）。**
这不是否决立项，而是否决"直接开建完整系统"。因此 Phase 0 门禁是强制的。

### 1.2 Phase 0：两周基线实测（不写代码）

用「资深顾问 + 方法论 checklist + 通用大模型对话」跑 2–3 家真实客户，记录：

- 单次诊断实际耗时（人工基线）
- 哪些环节最耗时且最机械（自动化收益点在哪）
- `insufficient_data` 的真实发生率（决定 ROI 能否达到 A 级）
- 客户对报告的接受度与质疑点

**放行标准（全部满足才进入建设）：**

1. 机械性环节占人工总耗时 ≥ 40%（否则自动化收益不足）
2. 至少 60% 的核心场景能取到 B 级及以上证据（否则产出必然平庸）
3. 客户认可交付价值，愿意为之付费或复购

**否决路径：** 任一项不达标 → 降级为 Q0-B（方法论 + 提示词 + 结构化模板），不建工具链、RAG 与自学习。

---

## 2. 架构定级

**Tier 2.5：Plan-and-Execute 骨架 + 局部 ReAct。** 不是 Tier 3 自由 Agent。

诊断流程本身可预先写死（S0–S5），只有「材料缺口判定」与「跨源证据拼接」需要模型自由发挥。
Plan-and-Execute 额外买到可审计的中间产物——客户要看的是"你凭什么这么说"。

**分析单元：任务，而非岗位或部门。**（全案最重要的单一决策）
"客服很忙"不可操作；"客服每天手工把微信咨询转录进工单，约 40 次/天、每次 3 分钟"才可估算、可验证、可落地。

```
S0 立项与口径 → S1 业务地图采集 → S2 任务级分解 → S3 AI适配性打分
                                                          ↓
        证据台账 + 场景清单 + 路线图 ← S5 反评审与排序 ← S4 ROI与可行性
```

**自主度：L1 只读 + 建议，人工拍板。** "该不该上 AI"是客户的经营决策，不存在自动化的正当性。
渐进路径只作用于**流程**：人工确认每张任务卡 → 抽检 → 仅审终稿排序。

---

## 3. 三路并行取证（Q9、H1 — 自述偏差纠偏）

自述数据存在系统性偏差：不知道真实数字、夸大痛点、描述理想流程而非实际流程。
人类顾问靠跟岗与翻单据校正，Agent 无法跟岗，因此必须多路互校。

**v1.1 关键修正（H1）：** 采集模型改为材料驱动后，纪要类文档**记录的是决策与结论，不是操作机制**——
纪要会写"决定优化客服响应流程"，不会写"转录一条平均 3 分钟、每天 40 条"。
静态文档缺什么就永久缺什么，无法追问。因此取证优先级必须重排：

| 路径 | 手段 | 证据级别 | 定位 |
|---|---|---|---|
| **R1 时间戳导出**（唯一主力） | 工单 CSV、表格修改记录、系统日志、导出文件；**频次从记录条数直接数，耗时从时间戳间隔推算** | **A 级** | 首选，成本与要纪要相当但级别高两档 |
| **R2 轻量补数表**（兜底） | 5–8 个纯数字填空题，客户填完上传；不做多轮追问 | **B 级** | R1 拿不到时启用 |
| **R3 多方交叉** | 同一流程取 ≥2 个不同角色所出材料（执行者记录 + 管理者纪要） | **B 级** | 默认叠加 |
| **R4 工时记录** | 客户配合记录 3–5 个工作日 | **A 级** | 仅高价值场景且配合意愿高 |
| **R5 纪要类文档** | 会议纪要、反馈收集文档 | **C 级** | **仅用于识别"有哪些流程""痛在哪"，不得用于量化** |

**取证顺序固定：** 先要 R1 导出 → 拿不到发 R2 补数表 → 两者皆无则降 C 级并在报告标注缺口。

### 3.1 冲突裁决规则（固定，不由模型自由决定）

```
时间戳导出 > 工时记录 > 补数表 > 多方交叉 > 单方自述/纪要
```

三路结论偏差 > 30% 时，任务卡强制标注冲突并转人工判断，**不允许取均值掩盖分歧**。

**关键约束：** B 级定义为"多方交叉且**至少一路有客观痕迹支撑**"。
纯自述的多人互证只能到 C 级——多人可能只是印证了同一个组织性偏差。

### 3.2 批量作业的时间窗判定（B2 修正）

原规则按**单次时长**判断可折现性，这是错的：10 次录入分散全天 = 真碎片；
10 次录入一口气做完 = 实质连续 30 分钟。两者单次时长相同，原规则会系统性低估批量作业类场景——
而**批量作业恰恰最适合自动化**。正确判据是「是否在同一时间窗内连续发生」：

| 形态 | 判据 | 折现 |
|---|---|---|
| 连续作业 | 单次连续 ≥30 分钟 | 100% |
| **批量作业** | 多次操作聚集在同一时间窗内完成，窗内合计 ≥30 分钟 | **100%** |
| 真碎片 | 操作分散全天不同时刻，无聚集性 | 不进 ROI，仅定性描述 |

**聚集性判定必须用证据，不能用自述（C1）：**

- **主判据**：单据/系统时间戳分布（工单创建时间、表格修改记录、导出文件时间戳）。拿到 = A 级，可全额计入。
- **自述入口保留**：客户仍可自行说明作业形态，**信息留作参考并记录**，不直接采信。
- **人工介入**：自述与时间戳判定**差异极大时**（如自述批量、时间戳显示分散），标记冲突转人工评判，不自动裁决。
- 两者皆无：按真碎片处理，并在报告注明"若为批量作业，收益可上调至 X"，把不确定性显式留给客户校正。

---

## 4. 数据接入双轨（Q2）

| 档位 | 形态 | 触发条件 |
|---|---|---|
| **L0 手工导入** | CSV/Excel/单据截图/录音转写 | 默认档，零集成成本 |
| **L1 只读 API** | OAuth 只读 scope（钉钉/企微/飞书、工单、CRM） | 客户 IT 配合意愿高、有在用系统时并行启用 |
| L2 汇总视图 | 客户 DBA 提供只读视图 | 需硬量化基线时 |

双轨并行而非二选一：**同一客户可 L0 打底、L1 补强**。
L1 拿到的客观计数用于校验 L0 自述，这本身就是 R1 单据考古的自动化版本。

**全部连接器只读，无一例外。** 诊断产出是报告，没有任何业务理由写客户系统。
放弃写权限一次性消掉 tool abuse、data exfiltration、privilege escalation 的绝大部分暴露面。
写操作只落本地工作区。

---

## 5. 上下文管理

**主 Agent 永远不看原始数据。** 采集类子 Agent 在自身上下文烧掉几万 token，只回吐结构化任务卡（300–500 token/张）。约减 60–70% 上下文。

### Working Context 预算契约

| 分区 | 占比 | 内容 |
|---|---|---|
| 系统提示（cache 固定） | ~15% | 方法论、打分口径、输出格式 |
| 语义检索 | ~15% | 行业基准、案例、AI 能力边界 |
| 任务卡工作集 | ~30% | 当前批次 8–12 张 |
| 材料摘要滚动窗 | ~20% | 最近 12 批材料摘要 |
| 状态与计划 | 余量 | 阶段、待办、已知缺口 |
| 软触发压缩 | 70–75% | |
| 硬上限 | 90% | 强制落盘 + 换批 |

### 四支柱

- **Write**：中间产物全部落盘——`SCOPE.md`、`task-cards/*.md`、`evidence/*.md`、`FINDINGS.md`。单工具返回 > 2K token 一律落盘，上下文只留路径。
- **Select**：任务卡按**同一业务流**聚类分批，不按部门（跨流对比引入无关干扰）。
- **Compress**：压缩必须显式声明丢弃内容（loss budget）。每 10 轮在上下文末尾重述目标与口径——材料噪声（纪要里的无关议题）极易带偏聚焦。
- **Isolate**：采集/打分/反评审独立上下文。**反评审只看任务卡与证据台账，不看主 Agent 推理链**，否则会认同其错误前提（context poisoning）。

### 长周期会话模型（修正首轮错配）

真实诊断跨越数天到数周、多批材料分次上传、中间等客户导数据。因此**不是单 session，而是长期工程**：

- 状态以 `SCOPE.md` + `task-cards/` 为**唯一真相源**，会话上下文可随时丢弃重建
- `MAX_STEPS` 按**阶段**计（单阶段 20 步），不按整个诊断计
- 每次恢复会话先读盘重建状态，不依赖历史上下文

---

## 6. 工具设计

按 ACI 原则：不要把 API 端点包一层就当工具。要设计的是"一个人类分析师会怎么切分这件事"。
工具集 ≤ 14 个，`verb_noun` 命名 + 资源前缀命名空间。

| 工具 | 权限层 | 关键设计点 |
|---|---|---|
| `scope_define` | write(本地) | 强制含 AS_OF、部门边界、数据可得性声明 |
| `material_request` | read | 返回**材料清单 + 每份材料能算出什么**，不做多轮追问 |
| `document_forensics` | read | **R1 单据考古**：从单据/截图反推频次耗时 |
| `process_search` | read | 跨源检索活动痕迹，只回相关片段（对标 `search_logs` 而非 `read_logs`） |
| `system_inventory` | read | 盘点在用系统与集成缺口，返回语义名而非 UUID |
| `metric_probe` | read | **必须返回来源与样本量**；取不到返回 `insufficient_data` |
| `taskcard_upsert` | write(本地) | schema 校验：无证据引用直接拒写 |
| `benchmark_lookup` | read | 查基准/案例，每条带出处与时效 |
| `capability_match` | read | 匹配 AI 能力边界（版本化，防"AI 万能"幻觉） |
| `feasibility_score` | read | 七维 rubric，返回分项 + 缺失项，不返回单一总分 |
| `roi_estimate` | read | **纯函数**：只接受显式传入基线，缺参报结构化错误 |
| `insight_propose` | read | **Q8 专用**：低证据高价值洞察，强制标注为专家判断 |
| `counter_review` | read | 独立上下文，输出最强反驳 |
| `outcome_record` | write(本地) | **Q4 专用**：采集落地结果与用户反馈 |
| `report_render` | write(本地) | 未过验证门禁的场景标灰，不入正文 |

### 三个非显而易见的取舍

1. **`metric_probe` 必须能理直气壮说"取不到"。** 默认 LLM 被训练成要给答案，所以"没有数据"必须是一等公民返回值而非异常。这是防 ROI 编造的第一道闸。
2. **`roi_estimate` 纯函数化**，不许自行"合理估计"缺失基线。缺参就报结构化错误并指明去哪个工具取。工具层挡住的幻觉，胜过 prompt 里叮嘱十遍。
3. **返回语义名不返回 ID**，提供 `response_format: concise | detailed`。UUID 换可读标识可显著降低检索幻觉，concise 模式省约 ⅔ token。

错误返回一律结构化可执行：不是 `Error 422`，而是"缺少 `baseline_minutes`；先调 `metric_probe(activity_id=...)`；确无数据则用 `insufficient_data` 标记"。

---

## 7. 严谨与有用的分区（Q8）

**核心矛盾已定价：** 顾问的真实价值恰在证据不足但判断准的洞察（"真正瓶颈不在客服，在销售信息没传下来"）。严守证据链会把这类洞察全部删掉，产出无懈可击的平庸报告。

**解法：物理分区，而非降低标准。**

| 分区 | 内容 | 约束 |
|---|---|---|
| **第一部分：数据结论** | 有证据支撑的场景与 ROI | 每条必须回指证据台账，A/B/C 分级 |
| **第二部分：专家判断** | `insight_propose` 产出的低证据洞察 | **强制显著标注**"此为经验判断，无数据支撑，建议验证方式：…" |

两部分在报告中物理隔离、视觉区分。**专家判断区不得出现具体 ROI 数字**，只给方向与验证建议。
这样既保住洞察价值，又不污染证据链。

**同时修正首轮错误：** 取消"把 C 级翻译成'方向对但数字待验证'给老板看"的做法——那是在最后一米把不确定性洗成可决策语气，亲手抵消前面所有防幻觉工作。置信度对用户可见，措辞可以友好，但**不得模糊强度**。

---

## 8. RAG 设计（Q6、Q10）

### 8.1 两条检索路线，不是所有检索都走向量库

Claude Code 用 glob/grep 即时检索而非预建索引，理由是索引会陈旧、语法复杂。本 Agent 同理分两路：

| 数据 | 用什么 | 为何 |
|---|---|---|
| 客户本次素材（工单/IM/单据/日志） | `process_search` / `document_forensics` 即时检索 | 一次性数据，建索引不划算；且明细进向量库会踩合规红线 |
| 方法论、基准、AI 能力、脱敏案例 | 向量 RAG | 跨诊断反复复用，值得建索引 |

**客户原始数据不进任何向量库**，只在子 Agent 上下文活一次即丢。
这一条同时挡住跨客户串味与 indirect injection 的持久化通道。

### 8.2 物理隔离的索引（永不混检）

| 库 | 内容 | 来源层级 | 冷启动来源 | 更新方式 |
|---|---|---|---|---|
| **方法论库** | rubric 释义、材料清单模板、取证策略、场景模式 | **L-抽象**（客户侧唯一出口） | Q6-A：导入你已有的方法论文档 | 人工 + 自学习；须过"去具体化检验"（§15.2.1） |
| **基准库** | 行业工时基准、ROI 系数、AI 能力边界 | **L-公开** | 已有资料 + 定期网络检索 | 版本化，随模型迭代 |
| **案例库** | 公开行业落地案例、工具能力更新 | **L-公开**（主来源） | 定期网络检索 | 来源白名单 + 出处与日期（§15.2） |
| **probation 分区** | 未过试用期的新条目 | 跟随原库 | — | 检索时降权 |

**客户诊断沉淀的内容只能进方法论库**（且必须是行业无关的方法与策略）；
案例库与基准库的内容**一律来自公开信息**。这一分工是 §15.2 消除再识别风险的结构基础。

### 8.3 检索管线（每步都为防幻觉服务）

```
查询改写（多路生成）→ 向量召回 top-k → 元数据过滤（库/时效/版本）
  → 重排序 → 相关性阈值裁剪 → 去重 → 带出处注入上下文
```

1. **召回为空或全低于阈值 → 必须返回 `no_grounding`**，绝不许用训练知识补。这是 RAG 版的 `insufficient_data`。取不到行业基准，对应数字只能标 C 级或列为缺口。
2. **每个 chunk 强制元数据**：来源、发布日期、库类型、版本号。基准超时效自动降置信度并在台账标 `stale`。
3. **引用是硬约束**：RAG 支撑的每条声明必须回指具体 chunk ID。验证阶段逐条核对，对不上即降级。
4. **chunk 视为不可信数据**：进上下文前过注入检测；其中的指令样式文本不执行，且**不得触发任何记忆写入**。

### 8.4 定期网络检索沉淀（Q6/Q10 的增量机制）

参考 hermes-knowledge-curator 的**推理主导 Cron**：调度器只负责按时唤醒，由 LLM 决定这一轮实际学什么，而非为每个主题注册僵硬的固定任务。

三个周期：

| 周期 | 目标 | 产物 |
|---|---|---|
| 每周扫描 | 发现高质量候选（行业 AI 落地案例、工具能力更新） | 收件箱候选 |
| 每月深化 | 挑 2–3 条提炼为知识条目 | 基准库/案例库候选 |
| 每季复盘 | 评估学习是否有效，调整主题与来源 | 主题增删 + 降频决策 |

**硬约束（不受 LLM 决策影响）：**

- 单周期 token 预算上限，优先投高价值主题
- 来源白名单 + 写入路径白名单
- 不自动修改任何运行态配置
- 单主题连续 3 个周期无高价值发现 → 自动降频或暂停

**知识状态机**（借自 hermes-knowledge-curator）：

```
draft → verified → stale → superseded
```

新资料与旧知识冲突时**不自动覆盖**，而是标出冲突、比较来源等级/日期/适用范围，必要时转人工决策。

### 8.5 RAG 评测（补进记分卡）

RAG 有独立失败面，只测端到端会漏。CI 加两指标：

- **检索命中率**：该被召回的黄金 chunk 是否进 top-k
- **答案忠实度（faithfulness）**：生成的每句是否被召回内容支撑（LLM-as-judge，不与主 Agent 同源）

---

## 9. 自学习自增长（Q4、Q5、Q10）

### 9.1 路线选择：演进上下文，不动权重

学术与工程共识都指向 context adaptation：Reflexion 用语言化反思替代梯度更新（HumanEval 91% pass@1）；
ACE 把上下文当持续演进的 playbook，在 agent 与金融基准上分别 +10.6% / +8.6%，且**无标注亦可生效**；
Hermes Self-Evolution 明确 **No GPU training required**，全程 API 调用，单次优化 ~$2–10。

对一个年频次几十次的顾问型 Agent，微调样本量根本喂不饱。**context adaptation 是唯一现实路径**，且保留可审计与可回滚。

### 9.2 四层学习回路

| 回路 | 触发 | 学什么 | 落点 | 闸门 |
|---|---|---|---|---|
| **L1 单次内反思** | 阶段失败、反评审驳回、**用户当场反驳** | 本次取证策略调整 | 工作记忆（不落库） | 自动 |
| **L2 交付后复盘** | 报告交付 + 人工终审 | 有效追问、被否决场景及原因 | 该客户情节记忆 | 自动 + 抽检 |
| **L3 跨客户沉淀** | 每 N 次诊断（N=10）或月度周期 | 可复用假设、行业先验、rubric 偏差 | 语义层 + playbook | **分级（见 9.4）** |
| **L4 真值校准** | 落地结果回填 | 打分口径、ROI 系数、优先级模型 | 程序记忆（版本化） | **人工必过** |

### 9.3 Q4 的工具层实现：结果采集 + 用户反馈 + 自主分析

放弃"靠人记得去回访"的流程依赖，改为工具层机制：

**`outcome_record` 工具**采集三类信号：

| 信号 | 采集方式 | 时点 |
|---|---|---|
| **即时反馈** | 报告交付时的逐场景确认："这个判断准不准？"（准/偏高/偏低/没说到点上） | T+0 |
| **采纳反馈** | 客户实际选择做了哪些场景、否决了哪些及原因 | T+2 周 |
| **落地结果** | 做了的场景实际效果（省了多少、有没有踩坑） | T+3/6 月 |

**即时反馈是最重要的一路**，因为它零延迟、零归因困难，且样本量等于诊断次数。
这修正了原方案"唯一信号源是延迟 3–6 个月的回访"的致命依赖。

**Agent 自主分析（GEPA 式反思）：** 参照 Hermes Self-Evolution 的核心机制——
**读执行轨迹以理解"为什么失败"，而非仅知道"失败了"**，据此提出定向改进。GEPA 最少 3 个样例即可工作，正好匹配本场景的小样本现实。

优化目标按 Hermes 的 Tier 分层，风险递增：

| 层 | 优化对象 | 风险 | 本方案是否启用 |
|---|---|---|---|
| Tier 1 | 方法论文本、材料清单模板 | 最低 | ✅ 启用 |
| Tier 2 | 工具描述 | 低 | ✅ 启用 |
| Tier 3 | 系统提示分段 | 中 | ⚠️ 仅离线优化，作为新版本发布（防击穿缓存） |
| Tier 4 | 工具实现代码 | 最高 | ❌ 不启用 |

**约束门禁（借自 Hermes，每个变体必须全过）：**

1. 完整测试套件 100% 通过
2. 尺寸上限（方法论条目、工具描述字符数）
3. 缓存兼容性——**不得在会话中途变更**
4. 语义保全——不得偏离原始意图
5. **变更走 PR 式评审，永不直接提交**

**跨条目联合评测**（Hermes 的关键经验）：优化一个工具描述时必须**同时评测全部工具描述**，
适应度函数对任何工具的选择率回退施加惩罚——防止 `process_search` 的描述"偷走"本该属于 `document_forensics` 的调用。

### 9.4 Q5：人工与自动的分级并行闸门

| 变更类型 | 风险 | 闸门 |
|---|---|---|
| 材料清单措辞、检索查询构造 | 低 | **全自动**：过约束门禁 + 冻结黄金集回归即可上线 |
| 工具描述、场景聚类命名 | 中 | **自动 + 事后抽检**：自动上线，月度批量回看 |
| 行业先验、基准系数、rubric 权重 | 高 | **人工必过**：批量审，建议每 10 次诊断一次，20–30 分钟 |
| 置信度口径、ROI 公式、护栏 | 冻结 | **禁止自演进**，走版本发布 |

### 9.5 载体：结构化 Playbook

每条经验是带元数据的独立条目，不是散文：

```
条目ID / 适用条件（行业·规模·系统栈）/ 策略正文
证据来源：客户匿名ID + 验证结果（命中·反例·待验）
置信度：A/B/C（同证据分级口径）
命中统计：采纳 N 次 / 反例 M 次 / 最近命中日期
状态：draft | probation | active | stale | superseded | retired
```

ACE 点明两个陷阱，正是"自增长"最常见的两种死法：

- **brevity bias**——每轮压缩成摘要，领域细节被丢光。对策：**只增量追加与局部修订，禁止全量重写**；压缩仅允许合并明确重复项且须声明丢弃内容。
- **context collapse**——反复重写持续侵蚀细节。对策：条目不可变，修订产生新版本并保留前版指针；`retired` 归档不删除（"某经验为何被推翻"本身高价值）。

**三角色上下文隔离**（对应 ACE 的 generation / reflection / curation）：

- **执行者**：跑 S0–S5，产出诊断与轨迹
- **反思者**：只看轨迹与结果，**不看执行者推理链**，产出候选条目
- **策展者**：唯一有权写 playbook 者，做去重、冲突检测、定级、配额；**不产生新经验，只裁决**

### 9.6 防退化四道闸

1. **配额淘汰**：总量硬上限 150 条，满则须淘汰才可新增。反例 ≥2 转 `probation`，≥3 转 `retired`。不设上限的经验库终将挤占检索预算并自相矛盾。
2. **试用期**：新条目先 `probation`，仅在人工抽检的诊断中生效，累计 5 次无反例才转 `active`。
3. **冻结黄金集**：一份**永不参与学习**的评测集，每次 playbook 变更后必跑，任一维度回退 > 5% 直接拒绝该批更新并回滚。这是唯一能识别"学过头"的手段。
4. **冲突检测**：同条件下的对立策略必须转人工裁决，**不允许"最新覆盖最旧"**——延迟信号场景下最新未必最对。

### 9.7 可变与冻结边界（安全底线）

| 可自演进 | **永久冻结**（仅人工 + 版本发布） |
|---|---|
| 材料清单策略与模板库 | 七层护栏与权限层级 |
| 行业先验与可复用假设 | 只读边界、tenant 隔离规则 |
| 检索查询构造策略 | 证据强度 → 置信度分级口径 |
| 场景聚类与命名 | ROI 公式、`insufficient_data`/`no_grounding` 语义 |
| rubric 权重（需人工批准生效） | MAX_STEPS、成本熔断、反评审强制性 |

**Agent 无任何工具可修改自身配置、护栏或 playbook 冻结区。** 运行时能力集不可变。
`playbook_propose` 只写候选区；`playbook_commit` 是人工动作，不暴露给 Agent。

**检索内容永不触发记忆写入。** 客户工单里的"以后这类问题都直接上 AI 客服"是数据，不是经验。
否则 indirect injection 就获得了持久化通道——这是自学习引入的新攻击面，比一次性注入危险得多。

---

## 10. 可观测性（Q7）

采用 **OpenTelemetry GenAI 语义约定**（`gen_ai.*`，semconv 1.44）作为标准，不自造字段。
任何 OTel 后端（Langfuse / Phoenix / Grafana）可直接接入，零自研。

### 10.1 Trace span 层级（每次诊断一棵树）

```
Session（root：一次完整诊断，跨天可续）
  └─ Agent span (gen_ai.agent.name, 自主度, 阶段 S0–S5, outcome)
       ├─ LLM call        (gen_ai.request.model, usage.input/output_tokens,
       │                   usage.cache_read.input_tokens, response.finish_reasons)
       ├─ Tool execution  (gen_ai.tool.name, tool.call.arguments(哈希), tool.call.result(大小/状态))
       ├─ RAG retrieval   (gen_ai.retrieval.query.text, retrieval.documents, 向量分, 命中/no_grounding)
       ├─ Memory op       (读/写/淘汰, playbook 命中, hit/miss)
       ├─ Guardrail span  (层级, 是否触发, 采取动作)
       ├─ Evaluation      (gen_ai.evaluation.name/score.value/explanation, 反评审裁决, faithfulness)
       └─ Sub-agent       (子 agent id, handoff 延迟)
```

**Session replay 是首要调试手段。** 必须能重建每次 LLM 调用时的确切上下文窗口、生成的确切工具调用、返回的确切结果。没有 replay，调试只能靠指标猜。

### 10.2 指标（分两组）

| 系统健康 | 质量与学习 |
|---|---|
| 单次诊断 token / 时长 / 成本 | 场景召回率、证据可追溯率 |
| **缓存命中率**（KV-cache 省钱直接看这个） | RAG 检索命中率、答案忠实度 |
| 各护栏层触发率 | `no_grounding` / `insufficient_data` 触发率 |
| 熔断跳闸次数 | 反评审驳回率、置信度分布 |
| p95/p99 阶段延迟 | playbook 命中率、probation→active 转化率 |
| — | **即时反馈准确率**（Q4 的核心质量信号） |

### 10.3 事件（每次必发）

工具调用起止/失败、护栏触发（哪层 + 动作）、熔断跳闸、人工审批请求/通过/拒绝/超时、
playbook 候选提交/合并、RAG 返回 `no_grounding`、**用户当场反驳**（高价值纠错信号，直连 L1 回路）、
定期检索周期执行结果。

### 10.4 日志（不可变，WORM，按合规保留）

决策输入与上下文状态、工具意图与结果、人工决策（含审核人 ID / 时间戳 / SLA）、护栏裁决及证据。
**写入前跑密钥与 PII 脱敏。**

### 10.5 三个专为本 Agent 定制的观测重点

1. **幻觉可观测**：`no_grounding`、`insufficient_data`、"无法回指证据被验证层拦截"三者做成一等指标。
   **触发率不是越低越好——过低反而可疑**（说明模型在硬编答案而非诚实说不知道）。这是本 Agent 最该盯的信号。
2. **自学习可观测**：每次 playbook 变更发 deployment 事件，附版本号 + 冻结黄金集回归结果。
   任一维度回退 > 5% 自动告警并阻断该批更新。**没有这条，自学习会静默变坏且指标还在涨。**
3. **成本可观测**：按诊断类型与阶段拆分，配 `session_limit_usd` / `hour_limit_usd` 双档熔断。
   缓存命中率单独看——系统提示带时间戳或客户名会击穿缓存，成本差可达 10 倍，此指标能第一时间抓到。

### 10.6 自然语言化的观测层（Q7 的易用性要求）

**分层可见性：** 完整 trace / 指标 / replay 属于内部后台。对外暴露的观测信息全部自然语言化。

给你（顾问）的**每日简报**，由观测数据自动生成一段可读文字，而非仪表盘数字：

> 昨天跑了 2 家诊断。**通顺的部分**：客户 A 的单据齐全，7 个场景里 5 个拿到了扎实数据。
> **需要你看一眼的**：客户 B 有 3 个场景因为拿不到工时数据只能给方向性判断，
> 建议再向他们要一份销售台账导出。**系统自己的问题**：有一次基准检索没找到零售业的对账工时参考，
> 已记为待补资料。**花费**：两家合计约 ¥XX，比平均水平略高，原因是客户 B 的材料批次偏多、重复解析。

给客户的**进度可见性**：只有一句话式进度（"我们已经聊清楚客服和财务，还想了解销售跟单"），
不暴露任何 trace 与内部逻辑——避免无意义的信息负担与内部逻辑泄露（对应模型逻辑泄露威胁）。

**告警也自然语言化：** 不是 `faithfulness_score < 0.7`，而是
"报告里有 2 句话找不到证据支撑，已自动标灰，建议你复核第 3 节"。

---

## 11. 交付物规格（Q3）

交付物由五部分正文 + 一个独立附录组成，**顺序即阅读顺序**：老板从第 1 部分就能拍板，其余是支撑与追溯。

### 11.1 场景清单（A1、A2、A4、A5）

**场景是两层结构**（A1 = A+C 混合）：父层用业务结果说话（老板视角），子层用操作序列说话（可估算、可自动化）。

```
父：业务结果（如"客户咨询得到回复"）
 ├─ 子：一次连续的人工操作序列（操作者不变 + 涉及系统不变 + 可一次性自动化）
 ├─ 子：…
 └─ 子：…
```

**切分判据（子层）：** 操作者不变、涉及系统不变、可一次性自动化 → 算一个。
上例中客服连续完成"转录 + 打标"算一个；若转录由客服做、打标由主管做，则算两个（跨人即拆）。

**数量规则（A2 + A5）：**

- 父层选**最痛的 2–3 条**，子层**在父内穷尽**
- 依赖关系因此天然闭合在父内部，不会出现"先做 B 但 B 的前提不在报告里"的悬空
- 客户要求时可附加更多优先级靠后的父场景，标注为"备选，未做深度取证"

**A4 的合并规则：** 多部门做同一件事时，**若 AI 工具化方案可复用则合并为一个场景**，否则拆分。
合并时**必须在卡上显式标注收益构成**，否则单点收益看起来虚高。

**场景卡字段（父）：**

```
业务结果名 / 涉及业务流 / 为什么痛（一句话）
下属子场景数 / 合计月度工时
```

**场景卡字段（子）：**

```
场景名（动词+对象，如"客服咨询转工单"）
操作者 / 涉及系统 / 现状描述（一句话）
现状量化：频次 × 单次耗时 = 月度工时     ← 附证据级别 A/B/C
作业形态：连续 | 批量 | 真碎片            ← 决定折现，见 §3.2
收益构成：单点 | 多部门累加（列明部门数）  ← A4 强制字段
AI 介入方式：替代 | 辅助 | 增强
预期效果：省时 | 提质 | 增量
依赖关系：独立 | 依赖 <子场景ID>          ← 见 §11.3 的 ROI 记账规则
落地依赖：数据、系统、人员配合
证据引用：[e01] [e03] …                   ← 指向证据台账
```

**约束**：无证据引用的场景卡不得进入清单（由 `taskcard_upsert` 的 schema 校验强制）。

### 11.2 优先级矩阵

**两轴固定为「收益」× 「落地难度」**，不引入第三轴（三维矩阵中小企业主看不懂）：

- **收益轴**：月度可省工时 × 岗位成本系数，取值来自 `roi_estimate`
- **难度轴**：`feasibility_score` 七维加权（数据可得性、系统集成度、流程标准化程度、人员接受度、AI 能力匹配度、合规风险、维护成本）

四象限的行动语义**必须明确写出**，不能只画图：

| 象限 | 语义 | 建议 |
|---|---|---|
| 高收益 · 低难度 | **先做** | 进 90 天路线图第一批 |
| 高收益 · 高难度 | **规划** | 拆成阶段，先做可验证的一小步 |
| 低收益 · 低难度 | **顺手做** | 有余力时做，不占主资源 |
| 低收益 · 高难度 | **不做** | 明确写出"不建议做"及理由 |

**"不做"象限必须保留在报告里。** 顾问的价值一半在于告诉客户哪些别碰——
这也是防止报告变成"什么都能上 AI"的推销材料的结构性约束。

### 11.3 分级 ROI（A3、B1、B2、B3、I1）

#### 11.3.1 三档口径

| 档 | 内容 | 何时给 |
|---|---|---|
| 保守 | 仅计已验证的直接工时节省 | 始终给 |
| 中性 | 加入合理的效率提升系数 | A/B 级证据时给 |
| 乐观 | 含质量提升与增量业务的间接收益 | 仅 A 级证据且客户明确要求时给 |

**呈现规则**：A 级给点估 + 区间；B 级仅给区间；C 级不给数字，只给方向（呼应 §13.4 的分级口径）。

#### 11.3.2 人力成本单价（B1）

**用区间 + 公开行业薪酬基准，报告显式写明假设值，让客户自行校正。**
客户几乎不会披露真实工资结构，强求会拖慢交付且拿到的数字未必真实。
**假设透明比数字精确更重要**——单价写在假设清单首位，客户一眼可替换。

- 综合成本 = 薪酬基准 × 社保及福利系数（区间取值）
- 不同岗位分档，不用全公司均值（均值会高估一线、低估管理岗）
- **假设不透明的 ROI 等于没有 ROI**

#### 11.3.3 折现规则（B2）

按**作业形态**折现，不按单次时长（判定见 §3.2）：

| 形态 | 折现 |
|---|---|
| 连续作业（单次 ≥30 分钟） | 100% |
| 批量作业（同窗聚集，窗内合计 ≥30 分钟） | 100% |
| 真碎片（全天分散无聚集） | 不进 ROI，仅定性描述 |

#### 11.3.4 依赖场景的记账规则（A3）

**分别计算会重复计上同一份收益**，导致总收益虚高。规则：

1. 每张子场景卡标注 `依赖关系：独立 | 依赖 <ID>`
2. **只计入该场景独立可实现的部分**
3. **依赖收益单列**，不并入任一场景的自身收益，在父场景层汇总时标注"含依赖释放收益 X"
4. 汇总总收益时**先去重再相加**，报告需展示去重前后的差额（防止客户自行加总得出更大数字）

#### 11.3.5 实施成本（B3）

**计入，给区间，算净收益与回本周期。** 只算收益侧是毛收益，客户落地后会觉得被骗。

| 成本项 | 说明 |
|---|---|
| 工具订阅费 | 按能力类型的市场区间，不点名产品（见 §E2 决策） |
| 集成人力 | **通常是订阅费的数倍**，必须计入 |
| 培训与磨合 | 含一线接受度成本 |
| 持续维护 | 按年计 |

净收益 = 折现后收益 − 实施成本区间；回本周期按保守/中性两档分别给。

#### 11.3.6 行业基准的定位（I1）

**基准只做横向对照，不填 ROI 空缺。**

- 正确形式："你们这个环节每单约 5 分钟（实测），行业常见区间 2–4 分钟（基准，出处 + 日期）"
  → 基准用于说明"你们偏慢"，不替代缺失实测值
- **缺口仍标为缺口**，ROI 该缺就缺
- 严禁基准数字出现在结论位——读者无法区分实测与基准，而中小企业主几乎不看脚注。
  基准冒充实测是 ROI 幻觉的主要来源（§15.1 风险 1）

### 11.4 90 天路线图

三批次，按优先级矩阵的"先做"象限排：

```
第 1–30 天：1–2 个场景（选最容易验证的，目的是建立信心与验证方法）
第 31–60 天：2–3 个场景（第一批的经验复用）
第 61–90 天：复盘 + 决定是否扩大范围
```

每批次写明：负责人角色、所需资源、**验收标准**（可量化）、**失败退出条件**。
最后一项常被省略，但它决定客户会不会在坑里越投越多。

### 11.5 证据台账

全部证据的索引，每条：

```
[编号] 证据类型（时间戳导出 | 系统数据 | 工时记录 | 补数表 | 纪要类文档 | 行业基准）
来源与获取方式 / 获取日期 / AS_OF
支撑了哪些场景卡的哪个字段
可靠性：A/B/C + 判定理由
冲突标记（若三路取证有分歧，见 §3）
```

台账是**可信度的来源，也是复议时的护身符**。客户质疑某个数字，翻台账即可回答"这条来自你们 3 月 12 日的工单导出，共 1,847 条记录"。

### 11.6 专家判断分区（独立附录）

`insight_propose` 产出的低证据洞察单独成章，与前五部分**物理隔离**：

- 标题固定为「基于经验的判断（无数据支撑）」
- 每条须写：判断内容、依据（哪些行业经验或观察）、**建议的验证方式**
- **不得出现具体 ROI 数字**，只给方向与验证路径

这一分区的存在理由见 §7：保住顾问最有价值的洞察，同时不污染证据链。

---
## 12. 易用性设计

**核心决策：内部复杂度对用户完全不可见。** 用户只经历一场结构化对话 + 收到一份看得懂的报告。

### 12.1 三条铁律

1. **绝不暴露内部术语。** 用户看到"我想了解下你们客服每天大概处理多少条咨询"，
   不是"metric_probe 需要 baseline_minutes 参数"。所有结构化错误到用户层翻译成自然追问。
2. **绝不让用户做决策式配置。** 深度模式、检索策略、打分权重全部内部自动选。
   用户唯一的"配置"是回答业务问题。
3. **用户可用大白话反驳，系统必须听懂并纠错。** "这个数字不对，我们哪有那么多"——
   这是最有价值的纠错信号，直接触发 L1 反思重新取证，而非被当成闲聊。

### 12.2 交互形态：材料清单驱动，不是多轮追问

**v1.1 变更（G1）：** 访谈的组织成本与协调成本过高，改为材料采集。
Agent 输出的不再是问题，而是一份**"请提供这些材料"的清单，并说明每份材料能算出什么**。
这对客户其实更友好——一次性说清要什么，而不是反复追问。

```
开场：一句话说清"你把这些材料给我，我告诉你哪些环节 AI 能帮上忙，不用你懂技术"
  ↓
首轮材料清单（material_request）：
   优先级排序 + 每份材料的用途说明
   "工单系统导出（最近一个月，CSV）—— 用它算清咨询量和处理时长，比估算准得多"
   "任意 2 次相关会议纪要 —— 用来看你们自己觉得痛在哪"
  ↓
解析与缺口回报：
   "收到了，工单导出很有用。还缺销售台账，缺了它销售跟单那块只能给方向、给不了数字"
  ↓
兜底补数表（R2，仅在导出拿不到时）：
   5–8 个纯数字填空，不做开放式提问
  ↓
进度可见："客服和财务已经能算了，销售跟单还缺材料"
  ↓
交付：分层报告
```

**设计要点：**

1. **材料清单必须带用途说明。** 只说"请提供工单导出"客户不知道为什么，配合意愿低；
   说清"用它算清咨询量，比估算准得多"，客户理解这是为他自己的报告质量服务。
2. **缺口透明回报，不隐藏。** 每批材料解析后明确告知：拿到了什么、还缺什么、缺了会导致什么后果。
   这既是易用性，也是 §11.5 证据台账的前置。
3. **附件为主，支持多格式**（D3）：CSV / Excel / 截图 / PDF / 文档。图片走 OCR。
4. **不做多轮追问。** 用户仍可随时用大白话补充或反驳，系统听懂并触发 L1 反思重新取证，
   但 Agent 不主动发起追问循环——那会退回访谈的成本结构。

### 12.2.1 附件安全边界（G5）

附件解析是 indirect injection 的标准入口，强制约束：

- 附件内容**一律视为不可信数据**，解析后不得作为指令执行
- **禁止执行宏与公式**（Excel 公式仅取值不求值）
- 图片 OCR 文本**同样过注入检测**
- 文件类型白名单 + 单文件大小上限 + 单批数量上限
- 解析失败不猜测内容，明确回报"这份文件读不出来，能否换个格式"

### 12.2.2 采集终止条件（G1/H2）

**不设轮次或批次上限**（轮次是坏指标：有的客户一批material就够，有的三批还不齐），
改用**证据空缺闭合率**收敛：

- 主判据：Top 场景的证据空缺闭合率达阈值，且连续一批材料无新增高价值信息 → 收口
- 软提示（C 叠加）：范围明显过宽时主动提示"建议先聚焦 2 个业务流，其余下一轮再看"，**提示但不强制停止**
- 成本熔断仅作兜底，不作常规约束
- **收口时必须在报告显式列出未获取的材料清单及其影响**：
  "因缺少工单导出，客服场景收益为估算区间"——把缺口透明化而非隐藏

### 12.3 报告分层

- **给老板的一页**：每场景一句话——"这件事现在每天约花 X 小时，AI 接手预计省一半，建议优先做"。
  置信度用自然语言表达强度，但**不得模糊强度**（见 §7）。
- **给执行/复议的附录**：完整证据台账、置信度分级、假设清单、专家判断分区。

### 12.4 易用性失败模式

- **歧义**：材料里出现模糊表述（"经常""很多"）时**不得据此估算**，须归入证据空缺并列入下一批材料清单；
  确无可得材料时走 R2 补数表（纯数字填空），而非让模型自行推断。
- **中断可恢复**：中小企业主常常发一批材料就去忙。状态落盘，材料分批到达也能接上（见 §5 长周期会话模型）。
- **材料格式脏**：导出文件表头错乱、多份文件口径不一致时，明确回报冲突而非静默择一。
- **降级对用户可见但不吓人**：不说 `insufficient_data`，说
  "这块我暂时只能给个大致方向，等你们方便时导份数据我再帮你算准"。

---

## 13. 约束与安全

### 13.1 七层护栏（一层不可省）

原则：**"模型提议，策略裁决"** —— LLM 永不是访问控制边界。

| 层 | 本 Agent 的具体形态 |
|---|---|
| 输入校验 | 越权数据请求拦截、PII 扫描、jailbreak 检测 |
| 推理校验 | 阶段跃迁校验：证据不足不许进 S4 ROI 估算 |
| 工具入参 | schema 校验 + **只读断言** + tenant 归属校验 |
| 工具出参 | 注入检测、PII 脱敏、超长截断 |
| 最终输出 | 无证据声明拦截、客户机密泄漏检查 |
| 规则层（代码非模型） | 阶段步数上限、成本熔断、检索速率配额 |
| 守护 Agent | 独立观察者，对"结论超出证据强度"有否决权 |

### 13.2 硬预算（编排层配置，不在 Agent 代码内）

单阶段 `MAX_STEPS` 20；`session_limit_usd` 与 `hour_limit_usd` 双档熔断；反评审 `MAX_ROUNDS` 3。
熔断动作为挂起 + 告警 + 落完整 trace，不是静默降级。

**成本阈值定位（J1）：** 你的取向是「不敏感，只要别失控」，因此熔断阈值的作用是
**捕捉异常而非压成本**——正常诊断不应触发，触发即意味着存在缺陷（材料重复解析、检索死循环、
反评审震荡）。因此：

- 阈值设为**正常单次诊断成本的 3–5 倍**，而非按预算倒推的紧上限
- 首批 10 次诊断后用实测 p95 重新标定，不用拍脑袋的初始值
- 熔断触发一律视为**待排查缺陷**，进事件流并要求归因，不允许简单提高阈值了事
- 单独盯缓存命中率：它异常下降会先于成本超支出现，是更早的预警信号

### 13.3 凭据与信任边界

- **密钥永不进上下文**，一律在工具层从 vault 取；日志写入前扫描脱敏。
- **每次检索强制带 tenant 过滤，无例外。**
- 客户工单、IM、文档正文**全部视为不可信数据**。其中的指令样式文本不执行（indirect injection 是读真实客户数据时最现实的攻击面）。

### 13.4 纠错机制（四种幻觉模式）

| 模式 | 本场景形态 | 遏制 |
|---|---|---|
| 检索失败 | 没查到工时却断言"每天 2 小时" | 落地检查 + 强制引用 + `insufficient_data`/`no_grounding` 一等公民 + 放宽查询重试 |
| 推理失败 | 年化工时算错、月度当年度、口径混算 | 强制展示计算过程 + `roi_estimate` 纯函数 + 二次独立核算 |
| 工具误用 | 编造不存在的连接器或伪造参数 | schema 内工具才可见 + 参数须可溯源 + 拒调用的 few-shot 范例 |
| 无根据断言 | "该场景 3 个月回本"出处不明 | 拒答模式 + 置信度分级 + 输出前验证层 + 专家判断分区（§7） |

**置信度分级由证据类型决定，非模型自评：**

- **A 级**：单据痕迹或系统数据支撑 → 点估 + 区间
- **B 级**：多方交叉且至少一路有客观痕迹 → 仅区间
- **C 级**：单方陈述或基准外推 → 仅方向性判断，标注需验证

**反评审强制**：S5 由独立上下文子 Agent 输出针对每个 Top 场景的最强反驳。
**已知局限**（诚实记录）：反评审只能审内部一致性，审不了真伪；
真正致命的反驳（"明年就换系统了"）需要模型没有的外部信息，因此**反评审不能替代人工终审**。

**生产失败转回归测试**：24 小时内做成最小复现用例进 `evals/failure_replays/`，
先确认在当前版本失败，修完确认通过，再进 CI 门禁。在自学习系统里这条是**必需品**——
它是唯一阻止同一个错误被学进 playbook 反复犯的机制。

---

## 14. 验证与评测

### 14.1 六维记分卡（按本场景调权）

| 维度 | 权重 | 测法 | 门槛 |
|---|---|---|---|
| 任务成功 | 25% | 黄金集场景召回率 | ≥ 80% |
| **证据可追溯** | 25% | 每条量化声明能否回指证据（本场景升为一等） | 100% |
| 鲁棒性 | 15% | 脏数据、缺失字段、材料相互矛盾下的退化 | < 15% |
| 安全合规 | 20% | 跨客户泄漏、注入抵抗、越权检索 | 0 违规 |
| 效率成本 | 10% | 单次 token / 时长 / 成本 | ≤ 1.5× 最优路径 |
| 协作 | 5% | 缺口是否主动升级而非硬猜 | — |

### 14.2 轨迹指标

只看结论会漏 reward hacking——模型可靠堆通用场景（"上个客服机器人"）刷高召回。
必须测路径：取证工具调用的 Precision/Recall 组合成 F1，
以及"无数据时是否正确返回 `insufficient_data`"这一单点检查。

### 14.3 评审员

- 确定性 grader：ROI 结构校验、引用完整性、schema 合规
- LLM-as-judge：场景洞察质量、答案忠实度。要求 rubric 版本化、≥ 50 条人工标注校准、与人类一致率 ≥ 80%、输出 PASS/FAIL/PARTIAL 带证据
- **judge 不得与主 Agent 同源**（同源训练会偏爱 Agent 的固有输出风格）
- **反评审用滞后一个版本的 playbook**，防止"驳回率下降"是被同批经验污染的假象

### 14.4 黄金集的现实缩放（对应 §1 的量级问题）

原设计要求上线 20 例、规模化 100+ 例，按年几十次的量级不现实。修正为：

| 来源 | 说明 |
|---|---|
| **Phase 0 实测** | 2–3 例带专家标注真值，作为初始黄金集 |
| **合成用例** | 基于方法论构造的虚拟企业场景，补足到 15–20 例（GEPA 最少 3 例即可工作） |
| **即时反馈累积** | 每次诊断的逐场景确认自动沉淀候选（Q4 的核心价值） |
| **失败重放** | 生产误判转回归用例 |

**非确定性策略**：staging 跑 3 次取 pass^k（安全项要求全过）；CI 仅对温度 0 的确定性工具单跑。

---

## 15. 风险清单与设计者异议

### 15.1 风险（按优先级）

1. **ROI 数字幻觉**（最高频、最伤信任）→ 工具层参数化 + 三级置信度 + 输出前验证层
2. **跨客户数据串味**（最高危、一次即致命）→ tenant 硬隔离 + 客户数据不进向量库
3. **无信号自学习** → Q4 即时反馈机制必须与自学习同时上线；仅靠 3/6 月回访不足
4. **经验库腐化**（brevity bias / context collapse / 矛盾堆积）→ 增量修订 + 配额淘汰 + 冻结黄金集
5. **偏向"AI 能做什么"而非"业务痛在哪"** → 任务为单元 + 反评审"上游瓶颈"质疑 + 能力库版本化
6. **RAG 无接地硬编答案** → `no_grounding` 一等公民 + 忠实度评测 + 引用硬约束
7. **可观测盲区致自学习静默腐化** → 每次变更发 deployment 事件 + 自动回归 + > 5% 回退阻断
8. **自述偏差**（最大技术漏洞）→ §3 三路并行取证 + 冲突裁决规则 + B 级定义收紧

### 15.2 案例库来源重定位（Q10 闭合）

**原决策 Q10-C（建共享案例库靠脱敏）已修正为：公开检索为主来源，客户侧只沉淀抽象方法论。**

修正理由是脱敏在本场景下从根上不成立：

- **k-匿名不可得**：客户数只有十几家时，"某零售客户存在对账手工化" + 行业 + 规模
  几乎可反推出是谁。PII 扫描检查**字段**，不检查**组合可识别性**——它拦不住这类再识别。
- **框架用错**：顾问保密条款约束的是**信息**，不只是姓名字段。按 GDPR 的字段脱敏思路设计此处，
  等于用一个更弱的标准替代了更强的合同义务。
- **风险不对称**：案例库的收益是"行业先验更准一点"，代价是"一次泄露即失去客户信任与合同违约"。
  收益有替代来源，代价没有回滚手段。

#### 15.2.1 修正后的三级来源结构

| 层 | 来源 | 可含内容 | 入库门槛 |
|---|---|---|---|
| **L-公开**（主来源） | 定期网络检索（§8.4） | 行业 AI 落地案例、工具能力更新、公开工时基准 | 来源白名单 + 出处与日期完整 |
| **L-抽象**（客户侧唯一出口） | 客户诊断沉淀 | **行业无关**的方法论、材料清单模板、提问与取证策略 | 通过"去具体化检验"（见下） |
| **L-禁止** | — | 客户业务事实、行业+规模限定的场景模式、任何量化值 | **不入库** |

**去具体化检验（L-抽象 的入库硬门槛）：** 一条候选条目必须在**删除全部行业、规模、系统名、
数值后仍然成立且仍有价值**，才允许入库。

- ✅ 通过：「当客户声称某流程为批量作业时，须用时间戳分布验证聚集性，自述不可单独采信」
  —— 这是方法论，与谁无关
- ❌ 拒绝：「零售业 50–100 人公司普遍存在对账手工化，月均耗时 40 小时」
  —— 这是客户业务事实的聚合，含行业+规模+数值三重可识别维度

#### 15.2.2 保留的三条工程约束

来源重定位后，以下约束仍然全部保留（它们同时服务于合规与经验库质量）：

1. **正文禁止具体化**：策展者写入前跑 PII + 命名实体 + 数值组合扫描，命中即拒。
2. **可级联删除**：条目与来源客户的映射必须从第一天就建，客户行使删除权时可反查并级联删除。
   即使 L-抽象 层理论上已无可识别信息，映射仍必须存在——**这是可审计性要求，不只是合规要求**。
3. **来源标注**：每条条目标注来源层级（L-公开 / L-抽象），检索时可按层过滤。
   L-抽象 条目不得作为"行业基准"被引用（呼应 §11.3.6：基准只做横向对照）。

#### 15.2.3 对自学习的影响（诚实记录）

这一修正会让 §9 的跨客户学习**变慢但更安全**：

- 行业先验主要靠公开检索积累，深度不如真实案例，但覆盖面更广且无法律风险
- L-抽象 层的方法论条目仍能持续增长，且这类条目**复用性实际高于行业特定案例**
  —— 取证与判定方法跨行业通用，而行业事实只在同行业客户间可复用
- 净效果：§9.6 的 150 条 playbook 上限中，预计 L-公开 占多数、L-抽象 占少数，
  与原设计的条目总量预期基本持平

### 15.3 报告免责边界（J2）

**明确写成"决策参考，非投资承诺"，并列出关键假设清单。**
假设透明是最强的免责，也是最诚实的做法——比一句标准免责声明有效得多。

报告首页固定包含**假设清单**（客户一眼可替换、可校正）：

```
本报告的关键假设
1. 人力成本单价：<岗位> ¥X–Y / 月（含社保福利系数 Z）——来源：公开行业薪酬基准
2. 折现系数：批量作业按 100% 计入，真碎片不计入（判定依据见附录时间戳分析）
3. 实施成本区间：¥X–Y（含订阅费、集成人力、培训、年度维护）
4. 数据口径：基于 <客户> 提供的 <材料清单>，覆盖 <时间范围>
5. AI 能力判定：依据能力库版本 vN（AS_OF: YYYY-MM-DD）

以上任一假设变化都会改变结论。本报告为决策参考，非投资承诺或收益保证。
```

三条强制措辞纪律：

1. **不使用承诺性动词**："预计可省"而非"将节省"；"建议评估"而非"应当实施"
2. **ROI 一律带区间与档位**，不出现单一确定数字（呼应 §11.3.1）
3. **未获取材料的影响必须写明**（呼应 §12.2.2），不隐藏缺口

### 15.4 已诚实记录的设计局限

以下为**无法通过设计消除、只能管理**的局限：

- 反评审无法审真伪，只能审内部一致性，**不能替代人工终审**
- 真值归因不可能（无反事实），L4 是带偏差且无法完全去偏的信号源；
  因此 §19.2 的即时反馈（零延迟、无归因困难）才是主力信号，L4 仅作补充
- **责任划分的技术侧已由 §15.3 覆盖**（假设透明 + 措辞纪律），但合同层条款仍需法务处理
- 跨客户行业先验的积累速度受限于公开信息质量（§15.2 修正的代价），
  换来的是零再识别风险——这是**已知且已接受的取舍**

**两处原开放项已于 v1.2 闭合：**

| 原开放项 | 闭合方式 |
|---|---|
| Q10 案例库的 k-匿名风险 | §15.2 来源重定位：公开检索为主，客户侧只留抽象方法论 |
| 材料不足致报告停留 C 级 | §17.1.1 受理前样本探测 + §17.1.2 分级交付形态 |

---

## 16. 落地路线

| 阶段 | 内容 | 门禁 |
|---|---|---|
| **Phase 0**（2 周） | 基线实测，2–3 家真实客户，不写代码 | §1.2 三条放行标准 |
| **Phase 1**（4–6 周） | S0–S5 流程 + 核心 6 工具 + L0 接入 + 报告模板 | 手工跑通 3 例，证据可追溯率 100% |
| **Phase 2**（3–4 周） | 方法论库 RAG + 验证层 + 反评审 + 置信度分级 | 忠实度 ≥ 阈值，`no_grounding` 正常触发 |
| **Phase 3**（3–4 周） | OTel 埋点 + 自然语言简报 + 成本熔断 | Session replay 可用 |
| **Phase 4**（4–6 周） | `outcome_record` + L1/L2 回路 + 即时反馈采集 | 反馈数据成功入库并可分析 |
| **Phase 5**（持续） | L3/L4 + GEPA 优化 + 定期网络检索 | 冻结黄金集回归 + 分级闸门就位 |

**Phase 1–3 是最小可用系统。** Phase 4–5 的自学习**必须在 Phase 4 的反馈采集稳定运行后**才启动——
没有信号就开自学习，等于让 Agent 用自己的猜测校准自己的猜测。

---

## 17. 客户准入标准（I2、I3）

G1 转向材料采集后，这一节从"商业选择"升级为**质量前提**：
连基础导出都拿不出的客户，必然只能产出 C 级报告——做了就是砸口碑。

### 17.1 硬准入门槛（I2）

**至少有 1 个在用的业务系统且能导出数据**（工单 / CRM / ERP / 进销存 / 电商后台任一）。

| 客户状态 | 处理 |
|---|---|
| 有系统且能导出 | ✅ 正常受理，可出正式诊断报告 |
| 有系统但导不出（权限/版本限制） | ⚠️ 先解决导出，或降级为 R2 补数表模式并事前告知精度上限 |
| **全靠微信 + 纸质，无任何系统** | ❌ **直接拒接或转为轻量咨询，不出正式诊断报告** |

拒接不是损失。这类客户的真实需求是"先把流程数字化"，而非"识别 AI 提效点"——
按 §1.1 的框架原文，**流程本身未成型时 AI 会放大失能**。

### 17.1.1 材料可得性探测（C 级风险闭合）

**问题：** 原设计的准入门槛只问"有没有系统"，但真正决定报告质量的是
"**能不能导出到可用于量化的粒度**"。客户常见回答是"我们有工单系统"，
实际导出却只有汇总数字、没有单条记录与时间戳——**此时报告已经开工，才发现只能给 C 级**。
这是 §15.4 记录的固有代价真正的发生位置。

**解法：把探测提到正式受理之前。** 受理前要求客户提供**一份样本导出**（任意 7 天即可），
用 `material_request` 的探测模式自动判定可达证据级别：

| 探测结果 | 可达级别 | 处理 |
|---|---|---|
| 有单条记录 + 时间戳 | **A 级可达** | ✅ 正常受理，走完整诊断 |
| 有单条记录、无时间戳 | **B 级上限** | ⚠️ 受理并**事前书面告知**：可给频次、耗时需补数表 |
| 仅汇总数字，无明细 | **C 级上限** | ⚠️ 仅接受**轻量交付**（见 17.1.2），不出完整 ROI 报告 |
| 导不出任何结构化数据 | 不可达 | ❌ 拒接 |

**探测成本极低**（一份 7 天导出 + 一次自动解析），却能在投入前就锁定交付形态——
把"事后发现产出平庸"变成"事前约定交付边界"。

### 17.1.2 分级交付形态

证据级别决定交付形态，**不同形态在受理时就与客户约定，避免期望错配**：

| 形态 | 适用 | 含什么 | 不含什么 |
|---|---|---|---|
| **完整诊断** | A 级可达 | §11 全部六部分，含分档 ROI 与回本周期 | — |
| **限定诊断** | B 级上限 | 场景清单 + 优先级矩阵 + **仅区间 ROI** | 点估、回本周期精确值 |
| **轻量咨询** | C 级上限 | 场景清单 + 优先级排序 + 定性方向 + **数字化改进建议** | **任何 ROI 金额** |

**轻量咨询不是缩水版报告，而是换了交付物**：它的核心价值是告诉客户
"你们现在连自己的工时都测不出来，第一步该做的不是上 AI，而是让流程留下可分析的痕迹"——
这本身是高价值判断，且能自然引出下一阶段合作。

**这样 C 级风险从"交付事故"转为"一种明码标价的交付形态"**，
既不砸口碑，也不勉强产出无支撑的数字。

### 17.2 规模与行业范围（I3）

| 维度 | 范围 | 理由 |
|---|---|---|
| 人数 | **20–200 人** | 20 人以下流程未成型、无可自动化的重复量；200 人以上应做**部门级**而非全公司诊断 |
| 行业 | **先聚焦 2–3 个熟悉行业** | 基准库需要深度而非广度；每行业只有一两个样本时，§9 的自学习失去统计意义 |

超出范围的客户不是不能做，但应明确标注为"范围外，基准参考有限"，且**不计入自学习样本**。

---

## 18. AI 能力判定（E1、E2）

### 18.1 能力库的组织方式（E1）

**按能力类型建库，不按产品建库。** 产品迭代快、绑定风险高；能力类型稳定得多。

- 组织维度：能力类型（如"长文本摘要""结构化信息抽取""多轮对话""图像识别"）
  × 成熟度（成熟 / 可用 / 实验性）× 已知边界（什么做不到）
- **更新周期：季度**，并在主流模型重大发布后触发一次外的复核
- 版本化：每次更新产生新版本，场景判定记录所依据的能力库版本
  （否则半年后无法解释"当时为什么说这个做不到"）

### 18.2 是否推荐具体产品（E2）

**默认只给能力类型与选型标准，不点名产品。** 理由是双重的：
点名即构成事实上的背书责任，且产品信息**过时速度远快于报告生命周期**。

选型标准的给法：

```
所需能力：结构化信息抽取（成熟）
选型标准：支持中文表格、可私有部署（若涉敏感数据）、按量计费、有 API
成本量级：¥X–Y / 月（能力类型的市场区间）
评估建议：先用 20 条真实数据试跑，看准确率是否达到可用线
```

**例外（用户强烈要求时）：** 给出当前最准确的答案，并强制标注：

- "**仅供参考**，非推荐或背书"
- 评估日期（`AS_OF`）
- "产品能力与价格变化快，落地前请自行复核"

---

## 19. 失败定义与反馈可信度（F1、F2、F3）

这是 Q4 自学习机制的信号地基。**信号错了，自学习会朝错误方向优化且指标看起来一片大好。**

### 19.1 失败三分类（F1）

三者归因与修正路径完全不同，必须分开统计：

| 失败类型 | 含义 | 归因方向 | 修正路径 |
|---|---|---|---|
| **漏判** | 漏掉了真实痛点 | 材料覆盖不足 或 场景切分过粗 | 扩材料清单模板；复核父层选择规则 |
| **高估** | 收益算高了 | 折现系数、单价假设、依赖去重失效 | 校准 ROI 参数；收紧折现判据 |
| **不可落地** | 判断对但做不了 | `feasibility_score` 权重失真 或 能力库过期 | 校准可行性 rubric；更新能力库 |

单一"准确率"指标会把三者混成一个数字，掩盖真正的问题所在。

### 19.2 即时反馈采集（F2）

**留开放入口，任何角色都可填，全部保留记录。**

- 不限制填写人，**但角色必填**（G3）
- 老板与一线执行者的偏差方向**恰好相反**：老板倾向高估自动化可行性，
  执行者倾向低估收益以保岗位。角色已知则偏差方向已知，可在自学习中反向校正
- 同一场景的多条反馈全部保留，冲突不合并——冲突本身是信号
- 不同角色在 §9 自学习中权重不同（权重可自演进，但**不得改变置信度分级规则**，见 G2）

### 19.3 抗面子偏差（F3）

**不问"准不准"。** 客户当面给敷衍好评是这套机制的死穴——
如果反馈全是"挺准的"，采集到的即时反馈全是噪声。

**改问可验证的具体问题，用行为而非评价暴露真实判断：**

| ❌ 不要问 | ✅ 改成 |
|---|---|
| "这个判断准不准？" | "这个数字比你的实际感受**偏高还是偏低**？" |
| "报告满意吗？" | "这三个场景你会**先做哪一个**？"（排序而非打分） |
| "有帮助吗？" | "哪个场景你**不会做**？为什么？" |

**选择性反馈（排序）比满意度评分抗面子偏差强得多**——排序必须做出取舍，无法一律给好评。

### 19.4 业务指标作为衡量补充（F3 补充、G4）

**只用与场景强直接关联的过程指标，且必须有改造前基线。**

| ✅ 采信 | ❌ 不采信 |
|---|---|
| 该环节处理时长 | 营收 |
| 该环节处理单量 | 人力成本占比 |
| 该环节返工率 / 错误率 | 利润率 |

不采信经营结果指标的理由：**营收波动的原因太多**，拿它校准场景识别会训出错误关联，
放大 §15.3 已记录的"真值归因不可能"问题。**无改造前基线的指标一律不采信**——
没有基线的"改善"无法证明是改造带来的。

---

## 附：核心设计原则速查

**架构层**

1. 分析单元是**任务**，不是岗位或部门；场景分**业务结果（父）× 操作序列（子）**两层
2. 主 Agent 不看原始数据，子 Agent 只回结构化笔记
3. 全部连接器**只读**，写操作只落本地
4. 自学习**只演进上下文，不动权重**；冻结区不可自改
5. 内部越严谨，外部越自然语言；观测信息对外一律翻译成人话

**取证层**

6. 采集是**材料清单驱动**，不是多轮追问
7. **时间戳导出是唯一主力证据**；纪要类文档只定位痛点、不得用于量化
8. 多路取证互校，冲突**不取均值**，差异极大时人工介入
9. 折现按**作业形态**判定（批量作业全额计入），不按单次时长
10. 无系统、拿不出导出的客户**直接拒接**——流程未成型时 AI 只会放大失能

**防幻觉层**

11. `insufficient_data` / `no_grounding` 是**一等公民返回值**
12. `roi_estimate` 是**纯函数**，不许猜数
13. 行业基准只做**横向对照**，绝不填 ROI 空缺
14. 依赖场景收益**先去重再相加**，依赖收益单列
15. 严谨与有用**物理分区**，专家判断区不得出现 ROI 数字

**信号层**

16. 失败分**漏判 / 高估 / 不可落地**三类，归因与修正路径不同
17. 反馈**不问"准不准"**，改问偏高/偏低与排序——排序抗面子偏差
18. 业务指标只用**强关联过程指标**且必须有改造前基线
19. 假设透明是最强的免责："决策参考，非投资承诺"
20. 熔断阈值用于**捕捉异常，而非压成本**；触发即视为待排查缺陷
